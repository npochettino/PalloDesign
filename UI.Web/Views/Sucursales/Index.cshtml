@model UI.Web.ViewModels.Sucursales.SucursalIndexViewModel
@{
    ViewBag.Title = "Sucursales";
}

@section scripts{
    <script>
        $(document).ready(function () {
            $('#sucursales').DataTable({
                "sDom":
                    '<"toolbar">frtip',
                stateSave: true,
                "language":
                {
                    "url": '@Url.Content("~/Content/JSON/Spanish.json")'
                },
                "columnDefs":
                    [{
                        bSortable: false,
                        aTargets: [-1]
                    }],
                "initComplete":
                    function (settings, json) {
                        $("div.toolbar").html(
                            '<a href="@Url.Action("Agregar", "Sucursales")" class="btn btn-success btn-sm"><span class="glyphicon glyphicon-plus"></span> Agregar</a>');
                    }
            })
        }
        );
    </script>
}

<div class="cabecera" style="margin-bottom: 20px;">
    Sucursales
</div>

<div class="grilla-listados">
    <table id="sucursales" class="table table-bordered table-condensed table-hover table-striped">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Descripcion</th>
                 <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model.Sucursales)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.Descripcion)</td>
                    <td style="text-align:center">
                        @*<a href="@Url.Action("Detalles", "Sucursales", new { id = item.Id })" class="btn btn-primary btn-xs">
                            <span class="glyphicon glyphicon-eye-open"></span>
                        </a>*@
                        <a href="@Url.Action("Editar", "Sucursales", new { id = item.Id })" class="btn btn-primary btn-xs">
                            <span class="glyphicon glyphicon-pencil"></span>
                        </a>
                        @*<a href="@Url.Action("Eliminar", "Sucursales", new { id = item.Id })" class="btn btn-danger btn-xs">
                            <span class="glyphicon glyphicon-trash"></span>
                        </a>*@
                    </td>
                </tr>
            }


        </tbody>

    </table>
</div>










