@model UI.Web.ViewModels.TiposMovimientosEfectivo.TipoMovimientoEfectivoIndexViewModel
@{
    ViewBag.Title = "Tipos de  Movimientos de Efectivo";
}

@section scripts{
    <script>
        $(document).ready(function () {
            $('#tiposMovimientosCaja').DataTable({
                "sDom":
                    '<"toolbar">frtip',
                stateSave: true,
                "language":
                {
                    "url": '@Url.Content("~/Content/JSON/Spanish.json")'
                },
                "columnDefs":
                    [{
                        bSortable: false,
                        aTargets: [-1]
                    }],
                "initComplete":
                    function (settings, json) {
                        $("div.toolbar").html(
                            '<a href="@Url.Action("Agregar", "TiposMovimientosEfectivo")" class="btn btn-success btn-sm"><span class="glyphicon glyphicon-plus"></span> Agregar</a>');
                    }
            })
        }
        );
    </script>
}

<div class="cabecera" style="margin-bottom: 20px;">
    Tipos de  Movimientos de Efectivo
</div>

<div class="grilla-listados">
    <table id="tiposMovimientosCaja" class="table table-bordered table-condensed table-hover table-striped">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Caja</th>
                <th>Suma</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model.TiposMovimientosCaja)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.Caja)</td>
                    <td>@Html.DisplayFor(modelItem => item.Suma)</td>
                    <td style="text-align:center">
                        @*<a href="@Url.Action("Detalles", "TiposMovimientosEfectivo", new { id = item.Id })" class="btn btn-primary btn-xs">
        <span class="glyphicon glyphicon-eye-open"></span>
    </a>*@
                        <a href="@Url.Action("Editar", "TiposMovimientosEfectivo", new { id = item.Id })" class="btn btn-primary btn-xs">
                            <span class="glyphicon glyphicon-pencil"></span>
                        </a>
                        <a href="@Url.Action("Eliminar", "TiposMovimientosEfectivo", new { id = item.Id })" class="btn btn-danger btn-xs">
                            <span class="glyphicon glyphicon-trash"></span>
                        </a>
                    </td>
                </tr>
            }


        </tbody>

    </table>
</div>










